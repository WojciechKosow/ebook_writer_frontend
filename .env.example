# Base URL of the Spring Boot backend (no trailing slash).
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8080
# Production example (set in Vercel project env vars):
# NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
